import React from 'react';

/**
 * Component for notifying a user an invalid path was navigated to
 *
 * @component
 */
const InvalidRoute = () => (
  <div className="not-found">
    <p>Nothing to see here...</p>
  </div>
);

export default InvalidRoute;
